import React from "react";

const Home = () => {
  return (
    <div>
      <h1>UNDER CONSTRUCTION</h1>
      <button onClick={() => (window.location.href = "/prediction")}>
        Predict
      </button>
    </div>
  );
};

export default Home;
